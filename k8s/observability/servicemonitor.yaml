apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: mindreply-monitor
  namespace: observability
spec:
  selector:
    matchLabels:
      app: backend
  endpoints:
    - port: http
      interval: 15s